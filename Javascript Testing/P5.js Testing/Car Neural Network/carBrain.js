class carBrain {

    layerSize = 5;
    numLayers = 3;

    numOutputs = 3;
    
    layers = [];
    weights = [];
    outputs = [];

    RELU(input) {
        return Math.max(0,input)
    }

    propograte(r,dir,theta) {
        
    }

}
